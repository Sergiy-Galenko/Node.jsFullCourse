const userProfile = {
    name: 'Sergii',
    commentsQty: 23,
    isLoggedIn: false,
}

const {name, commentsQty} = userProfile
const {isLoggedIn} = userProfile

console.log(name);
console.log(isLoggedIn);